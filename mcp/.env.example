# Copy to .env and fill in your values.
# Used by both the MCP server and the memory_chat_agent binary.

# Gemini (for agent + embedder when testing locally)
GEMINI_API_KEY=your_gemini_key
LLM_PROVIDER=gemini
EMBEDDER_PROVIDER=gemini
AGENT_PROVIDER=gemini
USER_ID=sumankhadka

# Qdrant (defaults)
# QDRANT_HOST=localhost
# QDRANT_PORT=6334
# QDRANT_COLLECTION=coding_memories
